#! /usr/bin/env node

var upgrader = require('../lib/upgrade');

// Check OS
if (!/^win/.test(process.platform)) {
    throw('This script upgrades npm on Windows, but the OS is not Windows.');
}

// Execute
upgrader.prepareUpgrade();
